import React from 'react';
import PropTypes from 'prop-types';

const DeputeVotes = ({ depute }) => {
  return (
    <div>

    </div>
  );
};

DeputeVotes.propTypes = { depute: PropTypes.shape({}).isRequired };

export default DeputeVotes;
